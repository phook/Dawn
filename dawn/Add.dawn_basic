define Add
  native javascript
    var self = this
    self._out_number = null
    self._in_begin = function(pipe)
      self._value = new Dawn.bigRat();
    self._in_go = function(pipe)
      if (pipe._out_number)
        pipe._out_number._call(self)
    self._in_number_$ = function(pipe, number)
      self._value = self._value.add(number._value)
  endnative
