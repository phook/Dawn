define For
  native javascript
    var self = this
    self._out_number = null
    self._in_begin = function(pipe)
      self._value = new Dawn.bigRat();
    self._in_number_from = function(pipe, number)
      self._value = number._value
    self._in_number_to = function(pipe, number)
       if (pipe._out_number)
           for(; self._value<number._value; self._value = self._value.add(1))
                pipe._out_number._call(pipe,self)
  endnative
