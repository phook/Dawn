define Divide
  native javascript
    var self = this
    self._out_number = null
    self._in_begin = function(pipe)
      self._value = new Dawn.bigRat();
    self._in_go = async function(pipe)
      if (pipe._out_number)
        pipe._out_number(self)
    self._in_number_dividend = function(pipe, number)
      self._value = number._value
    self._in_number_divisor = function(pipe, number)
      self._value = self._value.divide(number._value)
  endnative
