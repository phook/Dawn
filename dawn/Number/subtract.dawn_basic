define Subtract
  native javascript
    var self = this
    self._out_number = null
    self._in_begin = function(pipe)
      self._value = new Dawn.bigRat();
    self._in_go = async function(pipe)
      if (pipe._out_number)
        pipe._out_number(self)
    self._in_number_minuend = function(pipe, number)
      self._value = number._value
    self._in_number_subtrahend = function(pipe, number)
      self._value = self._value.subtract(number._value)
  endnative
