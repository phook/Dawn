define Concatenate
  native javascript
    var self = this
    self._out_string = null
    self._in_begin = function(pipe)
      self._value = ""
    self._in_go = async function(pipe)
      if (pipe._out_string)
        pipe._out_string(self)
    self._in_string_$ = function(pipe, s)
      self._value += s._value
  endnative
