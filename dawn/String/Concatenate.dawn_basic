define concatenate
  native javascript
    this._out_string = null
    this._in_begin = function(pipe)
      this._value = "";
    this._in_go = function(pipe)
      if (this._out_string)
        this._out_string(this)
    this._in_string_$ = function(pipe)
      this._value += pipe._value;
  endnative
