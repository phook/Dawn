define String
  output string
  input all string
    native javascript
      self._value = string._value;
    endnative
  input go
    native javascript
      if (this._out_string)
        this._out_string(this);
      if (this._out_resource)
        this._out_resource(this);
    endnative
  input instanciate
    native javascript
         var newObject = this._clone();
         return Object.assign({_value:decodeURIComponent(instanciate._value)},newObject);
    endnative